package Assessment;

import java.sql.*;

public class CompetitionDB implements {
   final Manager manager = new Manager();

    //this allows user to a new competitor
    manager.addCompetitorToDB(Competitor(100, 22, new Name("Alice Green"), "UK" ));
}
